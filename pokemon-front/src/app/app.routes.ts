import { Routes } from '@angular/router';
import { PokeSearcherComponent } from './components/poke-searcher/poke-searcher.component';

export const routes: Routes = [
    { path: '', component: PokeSearcherComponent }  // Ruta por defecto
];
